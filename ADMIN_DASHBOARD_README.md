# داشبورد ادمین - پلتفرم مزایده و مناقصه

## ویژگی‌های داشبورد ادمین

### 🔐 دسترسی ادمین
- فقط کاربران با `is_staff=True` می‌توانند به داشبورد ادمین دسترسی داشته باشند
- لینک ادمین در هدر برای کاربران ادمین نمایش داده می‌شود

### 📊 نمای کلی (Overview)
- **آمار کاربران**: تعداد کل، فعال، پریمیوم
- **آمار مزایده‌ها**: تعداد کل، فعال، تکمیل شده
- **آمار پیشنهادات**: تعداد کل و مجموع مبالغ
- **آمار اعلان‌ها**: تعداد کل و نخوانده
- **نمودارهای تعاملی**: آمار روزانه و کلی

### 👥 مدیریت کاربران
- مشاهده لیست تمام کاربران
- جستجو در کاربران
- فیلتر بر اساس نوع اشتراک و وضعیت
- عملیات گروهی: فعال/غیرفعال کردن
- ویرایش اطلاعات کاربران
- صادرات داده‌ها

### 🏷️ مدیریت مزایده‌ها
- مشاهده لیست تمام مزایده‌ها
- جستجو در مزایده‌ها
- فیلتر بر اساس وضعیت
- عملیات گروهی: فعال/غیرفعال کردن
- ویرایش اطلاعات مزایده‌ها
- صادرات داده‌ها

### 💰 مدیریت پیشنهادات
- مشاهده لیست تمام پیشنهادات
- فیلتر بر اساس مزایده و پیشنهاددهنده
- ویرایش و حذف پیشنهادات
- صادرات داده‌ها

### 🔔 مدیریت اعلان‌ها
- مشاهده لیست تمام اعلان‌ها
- فیلتر بر اساس کاربر، نوع و وضعیت خوانده شدن
- عملیات گروهی: علامت‌گذاری به عنوان خوانده شده
- ویرایش و حذف اعلان‌ها
- صادرات داده‌ها

### 💱 مدیریت ارزها
- مشاهده لیست تمام ارزها
- اضافه کردن ارزهای جدید
- ویرایش نرخ ارزها
- حذف ارزها
- صادرات داده‌ها

## API های ادمین

### داشبورد
- `GET /api/admin/dashboard/` - آمار کلی سیستم

### مدیریت کاربران
- `GET /api/admin/users/` - لیست کاربران
- `GET /api/admin/users/{id}/` - جزئیات کاربر
- `PUT /api/admin/users/{id}/` - ویرایش کاربر
- `DELETE /api/admin/users/{id}/` - حذف کاربر

### مدیریت مزایده‌ها
- `GET /api/admin/auctions/` - لیست مزایده‌ها
- `GET /api/admin/auctions/{id}/` - جزئیات مزایده
- `PUT /api/admin/auctions/{id}/` - ویرایش مزایده
- `DELETE /api/admin/auctions/{id}/` - حذف مزایده

### مدیریت پیشنهادات
- `GET /api/admin/bids/` - لیست پیشنهادات
- `GET /api/admin/bids/{id}/` - جزئیات پیشنهاد
- `PUT /api/admin/bids/{id}/` - ویرایش پیشنهاد
- `DELETE /api/admin/bids/{id}/` - حذف پیشنهاد

### مدیریت اعلان‌ها
- `GET /api/admin/notifications/` - لیست اعلان‌ها
- `GET /api/admin/notifications/{id}/` - جزئیات اعلان
- `PUT /api/admin/notifications/{id}/` - ویرایش اعلان
- `DELETE /api/admin/notifications/{id}/` - حذف اعلان

### مدیریت ارزها
- `GET /api/admin/currencies/` - لیست ارزها
- `GET /api/admin/currencies/{id}/` - جزئیات ارز
- `PUT /api/admin/currencies/{id}/` - ویرایش ارز
- `DELETE /api/admin/currencies/{id}/` - حذف ارز

### عملیات گروهی
- `POST /api/admin/bulk-action/` - عملیات گروهی
- `GET /api/admin/export-data/` - صادرات داده‌ها

## نحوه استفاده

### 1. ورود به سیستم
- با حساب کاربری ادمین وارد شوید
- لینک "ادمین" در هدر نمایش داده می‌شود

### 2. مشاهده آمار کلی
- در تب "نمای کلی" آمار کامل سیستم را مشاهده کنید
- نمودارهای تعاملی برای تحلیل روند

### 3. مدیریت داده‌ها
- از تب‌های مختلف برای مدیریت هر بخش استفاده کنید
- از فیلترها و جستجو برای یافتن موارد مورد نظر استفاده کنید
- عملیات گروهی برای مدیریت چندین مورد همزمان

### 4. صادرات داده‌ها
- از دکمه "صادرات" برای دریافت فایل JSON استفاده کنید
- فایل‌ها با تاریخ در نام فایل ذخیره می‌شوند

## امنیت

- تمام API های ادمین نیاز به احراز هویت دارند
- فقط کاربران با `is_staff=True` می‌توانند دسترسی داشته باشند
- تمام عملیات در لاگ‌ها ثبت می‌شوند

## اطلاعات ورود ادمین

```
ایمیل: admin@example.com
رمز عبور: admin123456
```

## نکات مهم

1. **پشتیبان‌گیری**: قبل از انجام عملیات حذف، از داده‌ها پشتیبان تهیه کنید
2. **تست**: تغییرات را ابتدا در محیط تست بررسی کنید
3. **لاگ**: تمام عملیات ادمین در لاگ‌ها ثبت می‌شوند
4. **دسترسی**: فقط کاربران مجاز می‌توانند به داشبورد ادمین دسترسی داشته باشند

## تکنولوژی‌های استفاده شده

### Backend
- Django REST Framework
- Django Admin
- JWT Authentication
- SQLite Database

### Frontend
- React.js
- Chart.js برای نمودارها
- Tailwind CSS برای استایل
- Axios برای API calls

### ویژگی‌های اضافی
- Responsive Design
- Dark/Light Theme
- Real-time Updates
- Export Functionality
- Bulk Operations 