# Trend - سیستم مزایده آنلاین

این پروژه یک سیستم مزایده آنلاین است که شامل فرانت‌اند React و بک‌اند Django می‌باشد.

## 🚀 راه‌اندازی پروژه

### پیش‌نیازها
- Python 3.8+
- Node.js 16+
- npm یا yarn

### راه‌اندازی بک‌اند

1. وارد پوشه بک‌اند شوید:
```bash
cd backend
```

2. محیط مجازی Python ایجاد کنید:
```bash
python -m venv venv
```

3. محیط مجازی را فعال کنید:
```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

4. وابستگی‌ها را نصب کنید:
```bash
pip install -r requirements.txt
```

5. مایگریشن‌ها را اجرا کنید:
```bash
python manage.py makemigrations
python manage.py migrate
```

6. سرور Django را اجرا کنید:
```bash
python manage.py runserver
```

بک‌اند در آدرس `http://localhost:8000` در دسترس خواهد بود.

### راه‌اندازی فرانت‌اند

1. وارد پوشه فرانت‌اند شوید:
```bash
cd frontend
```

2. وابستگی‌ها را نصب کنید:
```bash
npm install
```

3. سرور توسعه را اجرا کنید:
```bash
npm run dev
```

فرانت‌اند در آدرس `http://localhost:3000` در دسترس خواهد بود.

## 🔧 تست اتصال

برای تست اتصال بک‌اند:

```bash
cd backend
python test_connection.py
```

## 📋 ویژگی‌ها

### احراز هویت
- ✅ ثبت‌نام کاربر
- ✅ ورود کاربر
- ✅ مدیریت توکن JWT
- ✅ به‌روزرسانی خودکار توکن

### مزایده
- ✅ ایجاد مزایده
- ✅ شرکت در مزایده
- ✅ پیشنهاد قیمت
- ✅ مدیریت وضعیت مزایده

### ارز
- ✅ نمایش نرخ ارز
- ✅ به‌روزرسانی خودکار

### اعلان‌ها
- ✅ اعلان‌های کاربر
- ✅ مدیریت اعلان‌ها

## 🔌 API Endpoints

### احراز هویت
- `POST /api/auth/register/` - ثبت‌نام
- `POST /api/auth/login/` - ورود
- `POST /api/auth/token/refresh/` - به‌روزرسانی توکن

### پروفایل کاربر
- `GET /api/profile/` - دریافت پروفایل
- `PUT /api/profile/` - به‌روزرسانی پروفایل

### مزایده
- `GET /api/auctions/` - لیست مزایده‌ها
- `POST /api/auctions/` - ایجاد مزایده
- `GET /api/auctions/{id}/` - جزئیات مزایده
- `POST /api/auctions/{id}/bid/` - پیشنهاد قیمت

### ارز
- `GET /api/currency-rates/` - نرخ ارز

### اعلان‌ها
- `GET /api/notifications/` - لیست اعلان‌ها
- `POST /api/notifications/{id}/mark-read/` - علامت‌گذاری خوانده شده

## 🛠️ تکنولوژی‌ها

### بک‌اند
- Django 4.x
- Django REST Framework
- Django Simple JWT
- SQLite (توسعه) / PostgreSQL (تولید)

### فرانت‌اند
- React 18
- Vite
- Tailwind CSS
- React Router
- Axios
- React Hot Toast

## 📝 نکات مهم

1. **CORS**: در حال حاضر CORS برای توسعه تنظیم شده است. برای تولید، تنظیمات امنیتی را بهبود دهید.

2. **توکن‌ها**: توکن‌های JWT در localStorage ذخیره می‌شوند. برای امنیت بیشتر، از httpOnly cookies استفاده کنید.

3. **پایگاه داده**: در حال حاضر از SQLite استفاده می‌شود. برای تولید، PostgreSQL توصیه می‌شود.

## 🤝 مشارکت

برای مشارکت در پروژه:

1. Fork کنید
2. Branch جدید ایجاد کنید
3. تغییرات را commit کنید
4. Pull Request ارسال کنید

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است. 